<%= form_with model: excursion do |form| %>
    <div>
        <%= form.label :name, "Nome: "%>
        <%= form.text_field :name %>
    </div>

    <div>
        <%= form.submit "Salvar"%>
    </div>
<% end %>